@model IEnumerable<UserSignUpLogin.Models.TblUser>

@{
    ViewBag.Title = "Index";
}

<h2>Dashboard</h2>

<table class="table">
    <tr>
        <p>Hello!!!</p>
        <th>
            @Html.DisplayNameFor(model => model.StrUserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StrUserName)
        </td>
    </tr>
}

</table>
